"""
Example domain configurations for REIM.

These modules hold domain-specific presets (phase types, labels, ...) that
are *not* part of the core model. The core stays domain-agnostic; import
from here when you want a ready-made configuration for a concrete use case.
"""
